# Helmod 之后：面向品质系统的 产线量化规划工具设计文档

## 背景、问题与价值主张

entity["video_game","Helmod","factorio planning mod"]长期以来被认为是游戏内最强的“产线量化/规划”工具之一：它能按目标产量计算所需配方、原料、机器、模块、插件塔（beacon），甚至涉及发电与耗电等系统项。其定位在官方 Mod Portal 上被明确描述为“用于规划工厂的助手，可计算原料、产物、机器、模块、beacons，并可计算电力生产/消耗”。citeturn3view2

但在 entity["video_game","Factorio: Space Age","factorio expansion 2024"]时代，“品质（Quality）”成为生产设计必须量化的新维度：品质通过独立的 quality mod 引入；官方 Wiki 将其标注为 Space Age 扩展专有特性，且 quality 可作为独立 mod 启用（与 Space Age 内容大多解耦，但仍要求拥有 Space Age）。citeturn9view2turn8search18turn1view3

Helmod 已尝试接入品质：在 “when you add quality” 的讨论中，作者回复“implemented in v2.0.7”，证明其确实在 2.0 之后纳入部分品质概念。citeturn11view0  
然而，从同一讨论区的反馈可以看到：品质相关能力仍存在明显缺口或边界未覆盖——例如“核电相关实体（反应堆/热交换器/汽轮机）切换品质时不计入品质加成”，作者回复“that must be reworked”；又例如“燃料被当作带品质燃料参与计算，导致品质循环（Nutrient ↔ spoilage）结果不符预期”，用户提出希望“燃料默认按无品质处理/或可忽略燃料”。citeturn11view1turn11view2  
这些现象直接指向你的诉求：不是“有没有品质开关”，而是要**在统一量化语义下，正确建模品质对产物分布、效率与反推链路（机器/模块/资源）的影响**。

你的价值主张非常清晰且符合 Factorio 的精神：  
你不是要替玩家“自动选最优解”，而是要提供一个**可比较、可验证、可扩展**的量化框架，让玩家在“多方案、多配置、多约束”之间自行权衡。

image_group{"layout":"carousel","aspect_ratio":"16:9","query":["Factorio Helmod mod UI production planning screenshot","Factorio quality module 3 legendary icon screenshot","Factorio recycler building screenshot"],"num_per_query":1}

## 权威机制基线：品质、模块、回收的不可妥协事实

这一节只陈述“必须与游戏一致”的规则（也是本工具权威性的根基），全部来自官方 Wiki/官方论坛生态，便于将来校对与回归测试。

品质在原版（启用 quality mod）中有 5 个层级：Normal、Uncommon、Rare、Epic、Legendary，并明确指出 Legendary 相对 Epic 是“两级提升”。citeturn10view2  
此外，部分品质层级在研究完成前不可生成（“Certain tiers of quality cannot be created until they have been researched.”）。这意味着你的工具必须把“已解锁的品质集合”当成全局状态（或方案参数），否则会出现“理论能出、游戏不出”的错配。citeturn10view2

**品质配方与“同品质原料”约束**是建模的关键简化点：官方 Wiki 明确写道，消耗物品的配方会为每个品质变体提供一个版本；在生产设施中设置该配方时，需要选择“原料品质”，并且**所有物品原料都必须为该指定品质**；纯流体配方没有品质变体；若产出物品，则默认 Normal，除非被品质模块影响。citeturn10view1  
这条规则直接决定了你的工具在 UI 与模型上应如何表达：**“同一个配方”在品质维度上等价于一组并行的“品质变体配方”**，而不是一个允许多品质输入自由混合的组合爆炸模型。

**品质模块、速度模块与有效品质概率 Q**：  
质量模块 3（Quality module 3）在效果上提供“提升产生高品质物品的能力（quality chance）”并带来 -5% 速度惩罚；更重要的是，模块本身也有品质，传奇（Legendary）品质的质量模块 3 单个可提供 +6.2% quality。citeturn9view2turn9view2  
速度模块则会带来“品质惩罚”（例如 Speed module 1 使质量能力 -1%），但“机器的 quality bonus 永不低于 0”，因此速度模块不会把产物降级，只会抵消质量模块带来的提升。citeturn9view1  
因此对工具而言，“有效 Q”应当被统一计算为：  
- 来自质量模块、插件塔、研究、实体品质等的 quality 正向贡献  
- 减去来自速度模块等的 quality 负向贡献  
- 下限截断为 0（不可为负）  
这一点与“同一个界面既支持不插品质模块，也支持插品质模块且结果正确”的统一语义强相关。citeturn9view1turn9view2

**品质提升的概率分布（核心矩阵）**：官方 Wiki 直接给出当已解锁传奇时的通式：给定机器的总品质概率为 Q，则从某品质输入到更高品质输出的概率以固定权重衰减（0.9、0.09、0.009…），并给出示例表。citeturn10view0  
这一表可以被直接抽象为“品质转移矩阵” \(M(Q)\)（后文详述），它是你整个量化模型中最重要、也最稳定的基础构件。

**生产力（Productivity）与品质的组合规则**：官方 Wiki 明确指出：生产力条满后产生的“额外产物”与当前配方一致；若机器使用了品质模块，则生产力产物的品质**与当前这次制作确定的品质相同**，因为“随机品质 roll 在制作开始时确定，之后每次生产力条完成都会复制该结果”。citeturn9view0  
这条规则的直接建模后果是：在“期望值线性模型”里，生产力主要表现为**对数量的乘法缩放**，而不改变品质分布矩阵的形状（只是把分布整体放大）。但同时它也提醒你：若想做更精细的离散/方差模拟，需要考虑“每次制作先定一次品质再复制”的批量相关性。citeturn9view0

**回收机（Recycler）的硬规则**：  
- 回收机的本质是“损耗性的配方逆运算”：平均损失 75% 原料，即平均返还刚好等于“做出同类物品所需原料的 25%”。citeturn17view0  
- 回收不输出流体，因此流体原料会被完全损失。citeturn17view0  
- 回收机有 4 个模块槽，但**不能使用生产力模块**。citeturn17view0  
- 不插品质模块时，回收得到的物品与被回收物品**同品质**；插品质模块时，回收输出的品质也会像普通配方一样进一步提升。citeturn17view0  
- 若物品没有可逆配方（或因多配方等原因被判为不可逆），回收机将以 25% 概率吐回“同一个物品”（可受品质模块加成），其余 75% 销毁。citeturn17view2  
- 更进一层，官方 Wiki 解释了回收配方是**基于现有配方程序生成**的，并指出“多可逆配方的物品会被设为不可回收以避免歧义”“多产物配方默认不可逆”“mod 可通过 `auto_recycle=false` 明确设为不可逆”。citeturn18view0  
这意味着：你的工具必须在“数据层/模型层”支持**回收配方选择与歧义处理策略**，否则任何跨 mod 的准确性都无法保证。

## 统一量化语义：把“品质”纳入同一个规划语言

你希望实现的不是“在原有规划器旁边再做一个品质小工具”，而是：  
玩家在同一个界面、同一种交互语义下建模产线——无论该产线是否使用品质模块——工具都能返回一致口径的：产量、消耗、机器数量、模块配置效果、以及反推需求。

要达成这一点，设计文档建议采用三个核心原则：

**用“物品×品质”作为统一的会计单位（commodity）**  
将每个物品 \(i\) 拆成 5 个“品质子物品” \(i^{(1)},…,i^{(5)}\)（对应 Normal…Legendary）。你在 UI 中仍然展示“同一个物品”，但内部账本是 5 维向量。品质未启用时等价于只有 \(i^{(1)}\) 这一维，其余维度恒为 0；这就保证了“无缝切换”。citeturn10view2

**把每一道工序（配方/采集/回收/发电等）视为线性变换**  
在“期望值”层面，绝大多数生产过程都可被表示为：  
\[
\mathbf{y} = \alpha \cdot \mathbf{T} \cdot \mathbf{x}
\]  
其中：  
- \(\mathbf{x},\mathbf{y}\) 是“物品×品质”的流量向量  
- \(\alpha\) 汇总速度、产能、生产力等对“数量”的缩放（生产力的期望效果可直接表现为数量因子）。citeturn9view0turn9view3  
- \(\mathbf{T}\) 捕捉品质在层级间的流动（例如品质模块带来的升品概率）。citeturn10view0

**用“品质变体配方”规避多品质输入组合爆炸**  
因为官方规则规定：消耗物品的配方在每个品质上都有变体，且物品原料必须全为该品质。citeturn10view1  
这使得“品质建模”从潜在的指数复杂度，降维为：对每条配方按品质复制 5 份（或按已解锁品质复制若干份），并且每份配方只消耗该品质的原料，产出同品质基准的产物（再叠加品质模块的升品矩阵）。这正是“统一语义能成立”的核心原因。

## 品质转移矩阵与单工序产量：权威公式落地

这一节给出你工具中最关键的数学对象（也是之后回收闭环的基石）。公式直接来自官方 Wiki 给出的概率表与机制描述。citeturn10view0turn10view1

### 品质转移矩阵 \(M(Q)\)

假设已解锁传奇（Legendary），机器的总品质概率为 \(Q\)。则一次加工（制作/回收/采集等）对品质的“转移”可写为 5×5 矩阵 \(M(Q)\)，其列代表输入品质，行代表输出品质；矩阵为上三角（品质不会下降），并且 Legendary 输入恒为 Legendary。该矩阵的元素由官方给出的通式确定：从 Normal 输入开始，升级到更高品质的概率分别为 \(0.9Q, 0.09Q, 0.009Q, 0.001Q\)，保持原品质的概率为 \(1-Q\)；更高品质输入则截断到可升级的更高层级。citeturn10view0

在你的设计文档中，建议把这张表当成“规范定义”，而不是“实现细节”。因为它来自官方 Wiki，且被用作示例与推导基础。citeturn10view0turn1view1

### 数量缩放因子

数量缩放来自三类来源：

- **速度类**：影响单位时间操作次数，不改变“每次操作的物质量守恒”。（属于 throughput 维度）
- **生产力类**：官方定义为“同样输入产生更多输出”，等价于输出整体乘以 \(1+p\)。并且当存在品质模块时，生产力额外产物复制本次已确定的品质结果。citeturn9view0turn16search14
- **回收损耗**：回收机在原料层面平均返还 25%，且流体完全损失；这是一类不可忽略的数量衰减。citeturn17view0

因此，对任意工序，你工具内部应明确计算两件事：  
1) “一次操作”在数量上的期望净倍率（含生产力与回收损耗等）  
2) “一次操作”在品质上的转移矩阵 \(M(Q)\)

这正是你想要的“统一交互语义”：玩家只是在同一界面里插模块、选机器、选配方；工具把它们统一翻译成上述两件事。

## 回收闭环的稳定解：从迭代到闭式解

你们前面讨论并希望固化的部分，集中在“低品质终产物进入回收+再造循环，最终稳定产出多少 5 星”的问题。这一节把该模型写成可复用的规范，既能覆盖“齿轮回收+再造”，也能覆盖“不可回收物的纯回收衰减洗一次”等变体。核心依据来自官方对回收返还、不可回收行为与品质矩阵的定义。citeturn17view0turn17view2turn10view0

### 问题定义

给定某个“目标物品” \(P\) 的品质分布输入（只回收 1~4 星，5 星保留不回收）。我们关心：

- 输入一批 \(P\) 的 1~4 星（向量 \(\mathbf{x}\)）  
- 在固定配置（回收机品质概率 \(Q_r\)，再造机品质概率 \(Q_c\)，再造机生产力 \(p_c\) 等）下  
- 无限次循环后，总计能产出多少 5 星 \(P\)

这是一个典型的“吸收态”问题：5 星是吸收态（进入仓库不再回收），1~4 星是暂态（会继续回收）。这与官方 Wiki “品质只会升不降”以及回收损耗导致“终将耗尽或升到传奇”的直觉一致。citeturn10view0turn17view0turn1view1

### 单圈变换矩阵 \(T\)

定义“一圈”=（回收一次 → 将回收产物按配方再造回 \(P\) 一次 → 5 星截留）。  
在期望值模型中，一圈可表示为：

\[
T \;=\; \lambda \cdot M(Q_r)\cdot M(Q_c)
\]

其中：

- \(M(Q_r)\)：回收过程中由品质模块产生的“品质转移”，官方明确指出回收输出可像普通配方一样被品质模块提升；不插品质模块时输出保持同品质。citeturn17view0turn10view0  
- \(M(Q_c)\)：再造过程中由品质模块产生的“品质转移”。citeturn10view0turn10view1  
- \(\lambda\)：数量倍率，至少包含回收损耗 0.25，以及再造端生产力等因素。回收端的 0.25 来自“平均返还正好等于制作同类物品所需原料的 25%”。citeturn17view0turn9view0

若再造端存在生产力（来自模块/研究/实体内建等），其期望效果可作为 \(\lambda = 0.25\cdot(1+p_c)\) 的一部分；同时，生产力额外产物复制本次品质 roll 的结果，使得“期望上等价于整体放大”。citeturn9view0turn9view3

### 从 \(T\) 切出 \(A\) 与 \(g\)

把品质层级按“低星（1~4）/高星（5）”分块。令 \(T\) 是 5×5（输入品质→输出品质），则：

- \(A\) 是 4×4：输入 1~4 星 → 一圈后输出仍在 1~4 星（继续回收的部分）  
- \(g\) 是 4×1（或 1×4 视向量约定）：输入 1~4 星 → 一圈后输出为 5 星（本圈毕业的部分）

这一切分完全对应你的语义：  
A = “留级回流”，g = “毕业入库”。

### 无限循环的闭式解

一圈产出 5 星的期望为 \(g\)，两圈为 \(Ag\)，三圈为 \(A^2g\)…  
所以总产出是几何级数：

\[
\text{总5星} = \mathbf{x}^\top \left(g + Ag + A^2g + \cdots\right)
\;=\;
\mathbf{x}^\top (I-A)^{-1} g
\]

只要 \(A\) 的谱半径 < 1（直觉上成立：回收损耗使每圈总量衰减，且品质只会上升或被销毁），该逆矩阵存在且结果有限。回收机“损失 75% 原料”的硬规则是保证“不会无限增殖”的关键。citeturn17view0turn10view0turn9view0

### 两个重要特例

**只洗一次（不可回收物 / 无再造）**  
若物品不可逆，回收机行为是“25% 概率吐回自身，75% 销毁”，并且可受品质模块加成。citeturn17view2turn10view0  
此时一圈就只有一次品质转移：

\[
T = 0.25\cdot M(Q_r)
\]

后续仍可按同样方式切出 \(A,g\)，并用 \((I-A)^{-1}g\) 得到总 5 星收益。

**流体参与的配方循环**  
回收机无流体输出，流体原料在回收中全部损失。citeturn17view0  
因此对于含流体的目标物品，简单的“回收→再造”闭环在物料守恒上不再只乘 0.25，还会额外消耗流体补给；你的工具必须把“流体损失”作为闭环成本单独暴露出来，否则会把“理论可自循环”错误地展示为“零成本循环”。

## 齿轮示例：把公式写成可校验的基准算例

这一节把上面公式连接到你们对话中最常用的示例（齿轮），形成一个“回归测试级别”的权威算例：未来无论前端换几次、实现怎么重构，只要这个算例不一致，就说明模型漂移。

**配方事实**：铁齿轮由 2 个铁板制作。citeturn15search0  
**机器槽位事实**：组装机 3 有 4 个模块槽。citeturn14view0  
**传奇质量模块 3 的单模块品质概率**：Legendary Quality module 3 单个提供 +6.2% quality。citeturn9view2turn1view1  
因此，如果组装机 3 与回收机都插满 4 个“传奇品质模块 3”，则两端 \(Q_r=Q_c=4\times6.2\%=24.8\%\)，这也正是官方 Wiki 在品质示例表中展示的一个典型 Q 值。citeturn9view2turn7view0turn10view0

在该配置下：

- 单次“品质转移”中，从 Normal 输入到 Legendary 输出的概率为 \(0.001Q=0.0248\%\)。官方示例表直接给出：Q=24.8% 时 Normal→Legendary 为 0.0248%。citeturn10view0  
- 齿轮回收返还 25% 原料，再造回齿轮的数量倍率为 \(\lambda=0.25\)（不考虑再造端生产力）。这是回收机“平均返还正好等于制作同类物品所需原料的 25%”的直接推论。citeturn17view0turn15search0

用上述 \(T=\;0.25\cdot M(0.248)\cdot M(0.248)\) 可得到一个确定的 \(A,g\)，进而得到确切的“低星输入折算为最终 5 星产出”系数向量 \(h\)：  
- 1 个 1 星齿轮进入循环，最终期望产生约 0.003231 个 5 星齿轮  
- 1 个 2 星齿轮约 0.011580  
- 1 个 3 星齿轮约 0.041133  
- 1 个 4 星齿轮约 0.126509  

这些数值不来自拍脑袋，而是对上述权威矩阵与回收倍率的直接代入。它们的意义是：在固定配置下，你可以把 1~4 星齿轮当作“有一个面值（最终可折算的 5 星期望）”，从而把循环压缩成一次点乘（线性映射）。该思想与官方 Wiki 教程使用矩阵表达“一圈循环的品质变换”并不断迭代（提高幂次）是一致的，只是你采用了更高效的闭式求和。citeturn1view1turn10view0turn17view0

## 系统级量化规划：让“有/无品质”在同一工具里自然共存

你最终要解决的不仅是某一个闭环，而是“从目标产量拆到矿”的全链路——且同一条链路中既可能有普通生产，也可能有品质模块、品质原料、回收闭环、甚至跨星球/特殊建筑的生产力来源。官方 Wiki 也明确指出：由于回收只返还 25%，通常更优的策略是在“靠近目标产物的上游”逐步提升品质；但若目标物存在可叠加的生产力研究，则“通过回收循环”可能变为无额外物料成本（忽略流体）的最优路径。citeturn10view0turn16search12  
同时，官方 “Tutorial:Quality upcycling math” 针对不同机器、不同品质层级给出“质量模块与生产力模块的最佳配比表”，并强调“回收机始终只装品质模块”。这从权威层面确认了你的记忆：**并非总是全品质模块最好，混搭生产力模块经常更省料**。citeturn1view1turn16search10turn17view0

因此，设计文档建议把全系统解算拆成两类“可组合的原语（primitives）”：

**链式原语（DAG/层级拆解）**  
适用于无循环或循环可被局部压缩的产线：  
- 每个配方的每个品质变体是一条独立工序  
- 品质模块提供的升品质通过 \(M(Q)\) 体现  
- 生产力通过数量缩放体现，且不改变分布形状citeturn9view0turn10view1  
在这种表述下，“无品质”只是 \(Q=0\) 且只存在 Normal 维度的特例，与“有品质”完全同构。

**循环原语（闭环压缩为等效工序）**  
对于你们当前最关心的“终产物回收刷星”，用上一节的 \(h\) 向量把闭环压缩成：  
- 输入：该终产物的低星流量向量 \(\mathbf{x}\)  
- 输出：5 星产物流量 \(= \mathbf{x}^\top h\)  
这能把原本需要迭代或数值模拟的系统，转化为一个“可嵌入全厂线性会计”的等效模块，从而支持你所谓的“反向计算需要多少机器/模块/资源”。

这两类原语组合起来，就实现了你强调的统一交互语义：  
玩家只是在做“选配方→设目标→插模块→加回收/分流”的常规操作；工具内部把它们翻译成一组线性约束/线性变换，再给出机器数、资源流与品质分布。

## 假设、边界与验证策略

为了让这份文档“可长期作为蓝图”，必须明确哪些是“模型假设”，哪些是“游戏硬事实”，以及你将如何验证二者一致。

你的闭环公式属于**期望值模型**：它输出的是长期平均意义下的品质产量与回流比例，而不是每分钟严格整数、也不是短时间内箱子里每种原料都很均匀。官方论坛关于“回收长期产生某些原料堆积、某些原料短缺”的讨论，直指这类系统会出现随机游走式的波动；因此实际工程必须用缓存、优先级、回流控制避免堵塞。citeturn0search7  
这类波动不是你的公式错误，而是“随机过程的方差”导致的工程现象；你的工具要做的是把**期望值与必要的工程提示**区分展示（例如提示“需要缓冲/会出现配比抖动”）。

另外三类边界必须在工具层面显式处理：

- **流体回收损失**：回收机无流体输出，流体原料全部丢失；含流体配方的回收闭环必然需要外部补液。citeturn17view0  
- **催化剂与生产力例外**：生产力通常不作用于“催化部分产物”，官方 Wiki 给出 Kovarex 等例子。若你的目标是“权威”，就不能把生产力简单视为对所有产物等比放大。citeturn9view0  
- **回收配方歧义与不可逆**：回收配方程序生成、多配方物品不可逆、mod 可关闭 auto_recycle 等都会改变“回收等价式”。你的工具需把“数据选择策略”与“结果可解释性”当成一等公民——至少要能告诉用户：某物品为何被判为不可逆、因此回收行为退化为“25% 吐回自身”。citeturn18view0turn17view2  

只要你在实现中把以上硬事实当作回归基线，并用本文的齿轮算例做“可重复的数值验算”，这份设计文档就能满足你提出的第 6、7 点：无论会话中断多少次，只要回到这份文档，就能迅速重建上下文——我们在做什么、目标是什么、机制依据是什么、数学公式是什么、以及如何扩展到全厂规划。
## 常规量化模型：从“品质闭环”走向“通用配方网络求解”

这一节用于固化我们前面围绕“原油三相 + 裂解链”所做的全部讨论。它解决的问题不是品质分布，而是**在品质模型已经成立之后，如何对任意线性配方网络做常规量化计算**。这一步是 Faculator 从“品质推导器”走向“通用量化规划器”的关键。

### 设计目标

给定一组用户目标流量（例如每分钟需要多少石油气、重油、轻油，或者更一般地说，某一批 ingredient 的下游需求量），求解器应当能够：

1. 计算为了满足这些目标，系统中每条配方每分钟需要执行多少次。
2. 根据单机吞吐能力，进一步换算出需要多少台设备。
3. 反推出所有上游输入 ingredient 的每分钟需求量。
4. 当严格精确解不存在时，仍然给出一个**满足全部目标的最小可行解**，允许出现冗余副产物，但不允许任何目标不足。

这里的核心立场是：

- 求解器不猜业务语义，不猜哪个目标更重要。
- 用户提供的目标集合，默认全部视为**同等级硬约束**。
- 若精确解不存在，则允许“超额满足”，但不允许“低于目标”。
- 默认返回“满足全部目标且冗余最小”的方案。

这套语义必须在代码层保持中立、统一、可推广，不能写成“原油特判”或“石油气优先”之类带业务语义的临时规则。

---

## 一、先把“量化”从设备层剥离：第一层未知量不是设备数，而是配方执行率

前面的讨论里，我们先后尝试过三种直觉：

1. 直接按“配方执行 1 次”去建模。
2. 改为按“1 台设备 1 分钟净产出多少”去建模。
3. 再进一步回到更底层，按“每分钟执行多少次该配方”去建模。

经过辨析，最终确认：

> **通用量化求解器的第一层未知量，应定义为各配方的“每分钟执行次数”，而不是设备数量。**

原因如下。

### 1.1 为什么不能直接把“设备数”当第一层未知量

设备数会立刻把一大堆实现细节耦合进主模型：

- 设备类型
- 设备品质
- 制造速度（crafting speed）
- 模块配置
- 插件塔覆盖
- 研究加成
- 配方制造时间

如果一开始就把设备数放进矩阵，那“工艺网络求解”和“设备实现换算”就会揉成一团。这样会导致：

- 同一个配方，换一种机器、模块、塔配置，主方程都要重写。
- 很难复用同一套求解器去比较不同设备实现方案。
- 无法清晰地区分“需求决定的工艺流量”和“为了实现该流量所需要的机器”。

### 1.2 为什么“每分钟执行多少次配方”是最干净的未知量

因为它天然完成了两个关键解耦：

- 与**设备实现**解耦：不需要先关心机器速度和模块。
- 与**具体产物单位**解耦：任何物品流量都可由“单次净变化 × 执行率”得到。

这时每条配方只需要提供两份信息：

1. **单次执行时**的净输入输出变化。
2. **单机每分钟能执行多少次**（这一步留到第二层做设备换算）。

于是第一层求解器只负责回答：

> 为了满足目标流量，各配方每分钟分别要执行多少次？

这才是最稳定、最可推广的抽象。

---

## 二、游戏微观是离散事件，量化模型宏观上做连续化近似

这里有一个重要但容易混淆的建模边界。

在游戏机制层面：

- 配方并不是“每时每刻连续产出”的。
- 一个配方要完整经历若干秒制作时间，完成时才会一次性吐出一批产物。
- 因此微观看，它是**离散事件系统**。

但在量化规划层面，我们关注的是：

- 每分钟需要多少产出。
- 每分钟需要多少原料输入。
- 长期稳定运行下，产线的平均吞吐是多少。

这时采用的是：

> **稳态、长期平均、连续流量近似。**

因此，“每分钟执行多少次配方”并不是说游戏真的连续执行了 0.3 次、0.8 次；它表示的是：

> 在稳态平均意义下，该配方的**平均执行频率**。

这一定义同时兼容：

- 微观上的离散配方完成事件；
- 宏观上的连续流量量化。

所以 Faculator 的主求解器应该明确定位为：

- **稳态平均模型**，而不是 tick 级离散仿真器；
- 关注长期平均产能，而不是短时间抖动、瞬态积压、管道充液、机械臂节拍等时序细节。

---

## 三、制造速度 / 配方时间 * 60 在数学上到底是什么量

这是前面讨论中一个非常关键的概念澄清。

玩家在手算量化时，常常会用：

`制造速度 / 配方时间 * 60`

来求“1 台设备 1 分钟能生产多少”。这里需要严格区分四种量：

### 3.1 配方时间

单位可理解为：

- 秒 / 次

意思是：完成一次该配方需要多少秒。

### 3.2 制造速度（crafting speed）

它不是物理学里的“速度”。

更准确地说，它表示：

- 机器对配方进度的推进倍率。
- 在建模上更适合作为**无量纲倍率**看待。

### 3.3 配方执行率

将“制造速度 / 配方时间”理解为：

- 单位时间内可完成多少次配方执行

其单位是：

- 次 / 秒

再乘以 60，得到：

- 次 / 分钟

因此，

> `制造速度 / 配方时间 * 60`

本质上得到的是：

> **单机每分钟的配方执行率（recipe execution rate）**

也可以称作：

- 配方吞吐率
- 单机执行频率
- 单机每分钟可完成的配方次数

### 3.4 物品流率

一旦知道：

- 单机每分钟可执行该配方 `r` 次；
- 该配方单次产出 `q` 个某物品；

则该设备对该物品的每分钟产量为：

- `q * r`

单位变为：

- 个 / 分钟
- 或液体 / 分钟

这一点非常重要，因为它说明：

- **主求解器的核心变量应是“配方执行率”**；
- **设备层只是在把执行率换成设备数量**。

---

## 四、原油问题的最小示例：单次净变化矩阵

为了把抽象说明彻底落地，我们用原油三相与裂解链做最小示例。

### 4.1 物料集合

先只看三种油品：

- 重油
- 轻油
- 石油气

按固定顺序记为商品空间：

\[
v = (\text{heavy oil},\ \text{light oil},\ \text{petroleum gas})
\]

### 4.2 配方集合

考虑三条配方：

- 高级原油加工（advanced oil processing）
- 重油裂解（heavy oil cracking）
- 轻油裂解（light oil cracking）

### 4.3 每个配方的“单次净变化列向量”

定义：

- 产出记正；
- 消耗记负；
- 不涉及记 0。

则三个配方分别可写成：

\[
r_{\text{advanced}} =
\begin{bmatrix}
H \\
L \\
G
\end{bmatrix},
\quad
r_{\text{heavy crack}} =
\begin{bmatrix}
-h \\
l \\
0
\end{bmatrix},
\quad
r_{\text{light crack}} =
\begin{bmatrix}
0 \\
-l_2 \\
g
\end{bmatrix}
\]

含义分别是：

- 高级原油加工单次执行：产出 `H` 重油、`L` 轻油、`G` 石油气；
- 重油裂解单次执行：消耗 `h` 重油，产出 `l` 轻油；
- 轻油裂解单次执行：消耗 `l_2` 轻油，产出 `g` 石油气。

将三列并排，就得到原油子问题的“单次净变化矩阵”：

\[
A =
\begin{bmatrix}
H & -h & 0 \\
L & l & -l_2 \\
G & 0 & g
\end{bmatrix}
\]

这张矩阵只表达一件事：

> 每个配方执行 1 次，会对三种油品造成什么净变化。

需要特别强调：

- 这时矩阵里全是**已知量**；
- 它还不是可解问题；
- 它只是“系数表”或“规则模板”。

---

## 五、为什么有了矩阵还不够：必须再引入未知量与目标向量

这是前面讨论里一个容易误解、但一旦想通就非常清楚的点。

矩阵 `A` 告诉你的只是：

- 每个配方执行 1 次，会怎样变化。

但真正的问题是：

> 为了达到目标产量，每个配方**要执行多少次**？

因此必须再定义未知量向量：

\[
x =
\begin{bmatrix}
x_{\text{advanced}} \\
x_{\text{heavy crack}} \\
x_{\text{light crack}}
\end{bmatrix}
\]

其中：

- `x_advanced` = 高级原油加工每分钟执行次数；
- `x_heavy crack` = 重油裂解每分钟执行次数；
- `x_light crack` = 轻油裂解每分钟执行次数。

同时定义目标需求向量：

\[
d =
\begin{bmatrix}
b \\
c \\
a
\end{bmatrix}
\]

其中：

- `b` = 每分钟目标重油流量；
- `c` = 每分钟目标轻油流量；
- `a` = 每分钟目标石油气流量。

这时原油子问题的严格等式模型就写成：

\[
A x = d
\]

展开后就是三行联立方程：

\[
H x_{\text{advanced}} - h x_{\text{heavy crack}} = b
\]
\[
L x_{\text{advanced}} + l x_{\text{heavy crack}} - l_2 x_{\text{light crack}} = c
\]
\[
G x_{\text{advanced}} + g x_{\text{light crack}} = a
\]

至此，问题才第一次真正变成“可解的方程组”。

用最直白的话说：

- 矩阵 `A` 是**配方规则**；
- 向量 `x` 是**我们要求的东西**；
- 向量 `d` 是**用户给定的目标**。

三者合起来，才构成一个完整求解问题。

---

## 六、A 路线与 B 路线：到底要不要把原料输入也放进主矩阵

前面的讨论里，我们专门辨析过两条建模路线。

### 6.1 路线 A：只跟踪目标/中间产物

例如只跟踪：

- 重油
- 轻油
- 石油气

则矩阵行数只有这三种物料。像原油这种上游基础输入，不进入主矩阵，而是在求出配方执行率后再派生计算。

它的优点是：

- 维度更小；
- 当前子问题更直观；
- 对“目标拆配方”很自然。

### 6.2 路线 B：把所有相关物流都放进主矩阵

例如同时跟踪：

- 原油
- 重油
- 轻油
- 石油气

这样原油也成为系统中的正式物料维度。

它的优点是：

- 表达更完整；
- 可以直接在统一框架里看到基础输入的净消耗；
- 以后加“原油上限”“外部供给”“受限资源”等约束会更自然。

### 6.3 最终结论

经过讨论，结论是：

- **如果只是原油三相这个局部问题，路线 A 足够自然。**
- **如果目标是做可推广的通用量化求解器，最终仍推荐采用路线 B 作为总体抽象。**

但需要注意一个经常被误解的点：

> “把某物料纳入矩阵”
> 不等于
> “该物料也必须像目标产物一样被精确满足”。

例如原油通常不是目标，而是：

- 一个求解结果中的净消耗；
- 或一个允许外部供应的上游输入；
- 或一个受上限约束的资源。

因此从通用架构看，推荐采用：

- **工艺层统一使用广义物料矩阵**；
- **目标集合由单独的约束层定义**。

这样既保留了表达完整性，也避免“把所有物料都误当成终产物目标”。

---

## 七、输入 ingredient 与设备都不是第一层未知量，它们分别属于“派生层”

前面我们还专门澄清过一个关键问题：

> 采用“每分钟执行多少次配方”作为第一层未知量之后，输入 ingredient 与设备数分别是什么关系？

结论如下。

### 7.1 与设备实现解耦

一旦第一层求出各配方执行率，设备层只需额外知道：

- 单台设备在当前配置下，每分钟能执行该配方多少次。

设第 `j` 条配方由对应设备承载，其单机配方执行率为 `c_j`，则设备数为：

\[
n_j = \frac{x_j}{c_j}
\]

这时：

- `x_j` 来自工艺求解层；
- `c_j` 来自设备、模块、塔配置；
- `n_j` 是设备层的派生结果。

### 7.2 输入 ingredient 不是独立未知量，而是配方流的直接派生

若某条配方单次消耗某 ingredient 数量为 `q`，而该配方每分钟执行 `x_j` 次，则此 ingredient 的每分钟需求量就是：

- `q * x_j`

因此输入并不是“另一个需要单独猜的未知量”，而是：

> **由配方执行率自动决定的上游需求。**

所以在模型层更准确的表述是：

- 第一层与设备实现**解耦**；
- 输入 ingredient 并非与第一层解耦，而是被第一层**内生化**为直接派生结果。

---

## 八、为什么严格等式模型会无解

原油三相与裂解链的讨论，最终把我们带到了一个更一般的问题：

> 用户给定一组目标流量时，精确等式 `A x = d` 可能根本没有解。

这是多产物、副产物链中的常态，而不是例外。

原因在于：

- 某些配方同时产出多个物料，比例固定；
- 某些中间物之间可裂解，但裂解方向单向；
- 用户给定的目标比例，未必恰好落在该配方网络的可达锥面上。

因此如果要求：

- 每种目标物料既不能多，也不能少；

则系统常常无解。

但在实际量化中，用户通常可以接受：

- 副产物多一些；
- 只要所有目标都至少达标；
- 多出来的东西让自己去处理。

这就意味着：

> Faculator 不应只实现“严格等式求解器”，还必须实现“最小可行解求解器”。

---

## 九、从等式升级到不等式：全部目标都至少满足

这是整轮讨论中最重要的建模升级之一。

### 9.1 语义收束

解算器不应该猜：

- 哪个是主目标；
- 哪个是副产物；
- 哪个业务语义更重要。

对于通用 ingredient 推导阶段，代码只能看到：

- 用户要求若干目标物料的最低流量。

因此底层解算器的正确语义应当是：

> **用户给出的所有目标，一律视为同等级硬约束。**

即：

- 所有目标都必须至少满足；
- 不允许任何一个目标不足；
- 若无法精确卡死，允许出现超额。

### 9.2 矩阵不等式形式

于是严格等式模型：

\[
A x = d
\]

升级为不等式模型：

\[
A x \ge d
\]

并且始终有：

\[
x \ge 0
\]

这里：

- `A x` 表示实际产量向量；
- `d` 表示目标需求向量；
- `A x \ge d` 表示每个目标都至少达标。

这正是“满足用户总目标保底”的形式化表达。

---

## 十、为什么只有矩阵不等式还不够：它只给可行域，不给唯一解

一旦写成：

\[
A x \ge d,
\quad x \ge 0
\]

问题就从“可能无解的精确方程”变成了“有可能存在很多可行解的不等式系统”。

但这时会出现一个新问题：

- 只要把各配方执行率不断放大，依然满足 `A x \ge d`；
- 因此单靠不等式，只能描述“哪些解可行”，却无法决定“选哪一个解”。

所以矩阵不等式负责定义：

> 什么叫满足需求。

但还必须再补一个目标函数，用来定义：

> 在所有满足需求的方案中，哪一个最好。

---

## 十一、显式引入冗余变量：把“允许超额”写成等式系统

为了更好地表达和展示“多出来多少”，前面我们又把不等式进一步等价改写为“带冗余变量的等式系统”。

定义冗余向量：

\[
r \in \mathbb{R}^m
\]

其中 `r_i` 表示第 `i` 个目标物料的超额部分，并要求：

\[
r \ge 0
\]

于是有：

\[
A x = d + r
\]

并同时满足：

\[
x \ge 0,
\quad r \ge 0
\]

这个写法与 `A x \ge d` 等价，但好处是：

1. 保留了熟悉的等式结构；
2. 冗余被显式记录出来；
3. 最终结果中可以直接展示“每个目标多出来多少”；
4. 更方便写出“最小冗余”的目标函数。

从用户视角看，这个模型最终给出的结果就是：

- 你想要每分钟 `d`；
- 实际系统会产出每分钟 `d + r`；
- 其中 `r` 是需要你自己处理的冗余。

这正好对应我们在讨论中反复强调的语义：

> 解算器负责量化计算；多出来的副产物如何消化，是玩家自己的工程选择。

---

## 十二、通用解算器不猜“主目标”，默认求“满足全部目标的最小可行解”

这里是整个讨论最终收束出的原则。

我们一度讨论过“是否应该紧着某个主目标，例如石油气”。但最终确认：

- 这不是底层解算器应该猜的事；
- 如果上层有业务语义，可由上层显式提供；
- 通用 ingredient 推导阶段不应内置这种主从关系。

因此，底层解算器默认要求的是：

> **满足全部目标流量的最小可行解。**

换句话说：

- 不是“某个主目标达标即可”；
- 不是“任意一个勉强能用的可行解”；
- 也不是“严格等式无解就直接报错”；
- 而是“所有目标至少满足，并尽量少多做”。

这一定义足够中立、通用、可解释，也最符合量化器的职责边界。

---

## 十三、目标函数：为什么默认应当最小化总冗余

当约束为：

\[
A x = d + r,
\quad x \ge 0,
\quad r \ge 0
\]

还必须补一个目标函数。

### 13.1 讨论过的几类选择

我们前面依次考虑过几种可能：

1. **最小化总冗余**：尽量少多做。
2. **最小化总配方执行次数**：尽量少开工。
3. **最小化上游基础资源消耗**：例如最小原油消耗。
4. **优先保证某个目标物，再最小化其它偏差**：这属于上层策略，不适合底层默认值。

### 13.2 最终建议

对于通用解算器，最中立、最可解释的默认目标函数应当是：

> **在满足全部目标的前提下，最小化总冗余。**

因为它恰好对应：

- 不猜业务价值；
- 不偏袒某个目标；
- 不强行解释“最省原油”一定比“最少副产物”更合理；
- 与“用户允许副产物冗余，但希望尽量少冗余”的语义完全一致。

可写为：

\[
\min\; \mathbf{1}^T r
\]

若要更一般一些，也可引入权重向量 `w`：

\[
\min\; w^T r
\]

其中：

- `w_i >= 0`；
- 默认可取全 1；
- 将来若上层允许用户显式配置偏好，再开放加权。

### 13.3 平局打破项

如果担心存在多个冗余相同的解，可追加一个很小的二级目标，用来最小化总配方执行率：

\[
\min\; w^T r + \lambda \mathbf{1}^T x
\]

其中 `\lambda` 为足够小的正数，仅用于在冗余相同的方案中选择系统规模更小的方案。

这样：

- 第一优先级仍是少冗余；
- 第二优先级才是少开工、少机器、少基础消耗。

这一点在工程上很实用。

---

## 十四、最终通用符号化模型

将前面所有讨论收束到一个可在文档与代码中长期使用的符号化形式，定义如下。

### 14.1 基本对象

设：

- 有 `m` 个被跟踪的物料；
- 有 `n` 条配方。

定义矩阵：

\[
A \in \mathbb{R}^{m \times n}
\]

其中：

- 行对应物料；
- 列对应配方；
- `A[i,j]` 表示“配方 `j` 执行 1 次，对物料 `i` 的净变化量”。

定义未知量：

\[
x \in \mathbb{R}^n
\]

其中 `x_j` 表示配方 `j` 每分钟执行多少次。

定义目标需求：

\[
d \in \mathbb{R}^m
\]

其中 `d_i` 表示物料 `i` 每分钟至少需要多少。

定义冗余向量：

\[
r \in \mathbb{R}^m
\]

其中 `r_i` 表示物料 `i` 的超额满足量。

### 14.2 严格等式模型（理论基线）

\[
A x = d,
\quad x \ge 0
\]

这表示“恰好满足目标”的理想情况，可作为理论推导与校验基线，但一般不够稳健。

### 14.3 通用最小可行解模型（推荐实现）

\[
A x = d + r
\]
\[
x \ge 0
\]
\[
r \ge 0
\]
\[
\min\; w^T r + \lambda \mathbf{1}^T x
\]

其语义为：

- 所有目标都必须至少满足；
- 允许超额；
- 默认优先让冗余最小；
- 若冗余相同，则倾向于更小的系统规模。

这就是 Faculator 在常规量化层的通用求解器形式。

---

## 十五、把原油子问题重新写成标准线性代数外观

为了与前文讨论的推导保持一致，也为了方便写入技术文档或实现注释，原油问题可以正式写成如下形式。

定义三条配方的单次净变化列向量：

\[
r_{\text{advanced}} =
\begin{bmatrix}
H \\
L \\
G
\end{bmatrix},
\quad
r_{\text{heavy crack}} =
\begin{bmatrix}
-h \\
l \\
0
\end{bmatrix},
\quad
r_{\text{light crack}} =
\begin{bmatrix}
0 \\
-l_2 \\
g
\end{bmatrix}
\]

设配方执行率分别为：

\[
x_{\text{advanced}},\ x_{\text{heavy crack}},\ x_{\text{light crack}}
\]

则总体满足：

\[
x_{\text{advanced}}\,r_{\text{advanced}}
+
x_{\text{heavy crack}}\,r_{\text{heavy crack}}
+
x_{\text{light crack}}\,r_{\text{light crack}}
=
\begin{bmatrix}
b \\
c \\
a
\end{bmatrix}
\]

进一步记：

\[
A =
\begin{bmatrix}
H & -h & 0 \\
L & l & -l_2 \\
G & 0 & g
\end{bmatrix},
\quad
x =
\begin{bmatrix}
x_{\text{advanced}} \\
x_{\text{heavy crack}} \\
x_{\text{light crack}}
\end{bmatrix},
\quad
d =
\begin{bmatrix}
b \\
c \\
a
\end{bmatrix}
\]

即可写成标准形式：

\[
A x = d
\]

若允许冗余，则升级为：

\[
A x = d + r,
\quad x \ge 0,
\quad r \ge 0
\]

并通过最小化 `r` 得到满足全部目标的最小可行解。

这就是我们整个讨论最终回到的“最初那种标准矩阵形状”的严格数学来源。

---

## 十六、整数设备问题：连续工艺解与离散落地的分层

前面我们还专门讨论过一个工程现实：

- 配方执行率可以是连续值；
- 但设备数量不可能是小数台。

### 16.1 为什么第一层仍然坚持连续解

因为第一层解的是：

- 稳态平均意义下，各配方需要多大吞吐能力；

这天然是连续量。

例如解出：

- 需要 3.2 台满速精炼厂的等效产能；
- 需要 1.7 台满速化工厂的等效产能；

这在工艺层是完全合理的，因为它表达的是“理论最小需求”。

### 16.2 设备离散化属于第二层工程处理

当要落地成可建造方案时，再进行：

- `ceil(n_j)` 向上取整；
- 复算取整后实际配方执行率与实际产量；
- 显示因此带来的新增冗余。

这时就能明确告诉玩家：

- 理论最小是多少；
- 实际要摆多少台；
- 摆整数台后会多出多少。

因此文档上应明确分层：

1. **工艺求解层**：连续配方执行率；
2. **设备实现层**：设备数量换算与向上取整；
3. **结果复算层**：整数设备对应的实际产量、实际冗余与基础输入。

这样最清晰，也最符合工程实现逻辑。

---

## 十七、从原油问题推广到任意线性配方网络

原油三相只是一个很好的最小示例，但这套求解器的目标绝不是“只会解原油”。它应当能够推广到任意满足固定比例输入输出的线性配方网络。

### 17.1 可推广的根本原因

只要某条配方满足：

- 固定的单次输入；
- 固定的单次输出；
- 不因当次运行状态而改变输入输出比例；

那么它都可以抽象为：

> 一个“单次净变化列向量”。

将所有配方拼成矩阵 `A`，再求各配方的执行率向量 `x`，就是统一框架。

### 17.2 以后扩展到更大系统时，只需要做的事

- 增加更多物料行；
- 增加更多配方列；
- 为每条配方填入对应净变化；
- 根据上层目标生成 `d`；
- 交给同一个解算器求 `x`。

也就是说：

- 原油链只是 `A` 的一个小子矩阵；
- 品质网络是更高维的 `A`；
- 回收闭环压缩后的等效模块，也只是 `A` 中的一列或一个可组合块；
- 不需要写任何“原油特判求解器”“品质特判求解器”。

Faculator 的目标应当是：

> **一个面向任意线性配方网络的、支持品质扩展与冗余求解的通用量化内核。**

---

## 十八、实现建议：工艺层与设备层严格分离

综合所有讨论，建议在实现架构上明确分为两层。

### 18.1 工艺层（求解器核心）

输入：

- 物料集合；
- 配方集合；
- 单次净变化矩阵 `A`；
- 目标需求向量 `d`；
- 可选权重 `w`。

输出：

- 各配方每分钟执行次数 `x`；
- 各目标冗余 `r`；
- 各物料的实际流量；
- 各上游输入的派生需求。

该层只做：

- 线性约束建立；
- 最小可行解求解；
- 与设备实现完全解耦。

### 18.2 设备层（实现映射）

输入：

- 工艺层给出的配方执行率 `x`；
- 每条配方对应的设备类型；
- 设备品质、模块、塔配置；
- 单机每分钟可执行该配方的次数 `c_j`。

输出：

- 连续设备数；
- 向上取整后的整数设备数；
- 取整后的实际产量与实际冗余；
- 取整后的电力、模块需求等。

这一分层使得：

- 同一套工艺求解可复用到不同设备配置比较；
- 同一目标可横向对比“裸机方案”“满模块方案”“不同 beacon 方案”；
- 主求解器逻辑始终稳定，不会被设备实现细节污染。

---

## 十九、本节结论

把这整轮讨论压缩成几条最核心结论，就是：

1. **第一层未知量不是设备数，而是各配方每分钟执行多少次。**
2. **矩阵 `A` 表示每条配方执行 1 次时的单次净变化；它本身只是已知系数表。**
3. **完整问题必须写成“已知矩阵 × 未知执行率 = 目标向量”的形式。**
4. **严格等式 `A x = d` 是理论基线，但对多产物链常常无解。**
5. **通用量化器应升级为“满足全部目标的最小可行解”模型：**
   \[
   A x = d + r,
   \quad x \ge 0,
   \quad r \ge 0,
   \quad \min\; w^T r + \lambda \mathbf{1}^T x
   \]
6. **输入 ingredient 需求由配方执行率派生；设备数量由执行率再映射而来。**
7. **连续工艺解与整数设备落地应明确分层处理。**
8. **这套框架不仅能解原油，也能推广到任意线性配方网络，并自然承接前文的品质矩阵与回收闭环模型。**

到这里，Faculator 的数学内核已经形成一条完整主线：

- 品质层：用“物品 × 品质”的线性变换描述升品与回收闭环；
- 常规量化层：用“物料 × 配方”的线性系统描述任意配方网络；
- 设备层：将连续配方执行率映射为具体机器、模块、塔与功耗。

这三层合在一起，才是真正意义上的“Helmod 之后”的统一量化规划工具。
