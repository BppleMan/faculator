护圈式重载按钮:金属护边(collar)→ 深井(well)→ 凸面键帽(cap,3-4px 磨圆棱角),键帽带 3px 实心侧壁作机械轴行程,按下沿轴沉底。主键金铜面,次级炭黑面,危险为暗红;ghost 为扁平细线框。

```jsx
<Button onClick={run}>应用约束并解算</Button>
<Button variant="secondary" size="sm">导出</Button>
<Button variant="danger">清空区块</Button>
```

变体 primary/secondary/ghost/danger;尺寸 sm/md/lg;`block` 整行;`icon` 传简单几何 SVG。文案用动词短语。