凹槽(inset)输入框,聚焦时琥珀描边+辉光;数值输入加 `mono`。

```jsx
<Input label="产线名称" placeholder="如:绿色科技包" />
<Input label="速率" mono suffix="/min" defaultValue="120" />
<Input error label="名称" defaultValue="重复的名称" />
```