[package]
edition = "2024"
name = "faculator-migration"
publish = false
rust-version = "1.85.0"
version = "0.1.0"

[lib]
# name = "faculator_migration"
path = "src/lib.rs"

[dependencies]
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread"] }
sea-orm-migration = { version = "2.0.0-rc", features = [
    "sqlx-sqlite",
    "runtime-tokio",
] }
